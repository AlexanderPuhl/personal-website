# Docker specific files
Dockerfile
docker-compose.yml
.dockerignore

# Git / Version control
.git
.gitignore

# Node modules (will be installed inside the container)
node_modules

# Adonis build artifacts / temporary files
build
tmp

# Environment files (should be managed via docker-compose)
.env
.env.example
.env.test

# OS specific files
.DS_Store
Thumbs.db